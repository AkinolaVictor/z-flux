import {Z_Text} from "z-flux-react";
                or
import VerticalScroll from '@/VerticalScroll';


// ensure to set a specific width (minWidth), and height (minHeight) for each of the child element 
<VerticalScroll animation={"SlideUp"}>
    <div 
        style={{ background: "blue, height: "100%",
            minWidth: 416px",
            display: "flex",
            justifyContent: "center",
            alignItems: "Center"
        }}
    >
        <p>Color is blue</p>
    </div>

    <div 
        style={{ background: "yellow, height: "100%",
            minWidth: 600px",
            display: "flex",
            justifyContent: "center",
            alignItems: "Center"
        }}
    >
        <p>Color is yellow</p>
    </div>

    <div 
        style={{ background: "green, height: "100%",
            minWidth: 200%",
            display: "flex",
            justifyContent: "center",
            alignItems: "Center"
        }}
    >
        <p>Color is green</p>
    </div>
</VerticalScroll>




<VerticalScroll animation={"Basic"}>
    {
        ["blue","red","green","brown","#333","purple","orange"].map((background, index)=>{
            return (
                <div 
                    key={index} 
                    style={{
                        background,
                        height: "100vh",
                        minWidth: index==3?"416px":"100%",
                        display: "flex",
                        justifyContent: "center",
                        alignItems: "Center"
                    }}
                >
                    <p>Section {index+1}</p>
                </div>
            )
        })
    }
</VerticalScroll>

            
<VerticalScroll animation={"SlideUp"} direction='backward'>
    <div style={{ background: "blue", height: "100vh", minWidth: "100vw", display: "flex", justifyContent: "center", alignItems: "center"}}>
        Color is Blue (1)
        // You can do anything here
    </div>
    <div style={{ background: "red", height: "100vh", minWidth: "100vw", display: "flex", justifyContent: "center", alignItems: "center"}}>
        Color is red (2)
        // You can do anything here
    </div>
    ...
    // You can keep adding more stuffs as you want.
</VerticalScroll>



const dt = [
    {
        style: {minWidth: "100%", height: "240px", background: "blue"},
        txt: "Sasd"
    },
    {
        style: {minWidth: "804px", height: "80px", background: "green"},
        txt: "Sasd"
    },
    {
        style: {minWidth: "320px", height: "280px", background: "#1C398E"},
        txt: "Sasd"
    },
    {
        style: {minWidth: "200px", height: "404px", background: "red"},
        txt: "Sasd"
    },
    {
        style: {minWidth: "100%", height: "360px", background: "#0D542B"},
        txt: "Sasd"
    },
    {
        style: {minWidth: "440px", height: "200px", background: "#8EC5FF"},
        txt: "Sasd"
    }
];

<VerticalScroll animation={"SlideUp"} startAnimation='bottom'>
    {
        dt.map((item, index)=>{
            return (
                <div key={index} 
                    style={{display: "flex", justifyContent: "center", alignItems: "center", ...item?.style}}
                >
                    <p>Page {index+1}</p>
                </div>
            )
        })
    }
</VerticalScroll>

